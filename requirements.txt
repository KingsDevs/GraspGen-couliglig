# GraspGen (Couliglig) requirements — pinned to a known-good environment.
# Tested with Python 3.10 + CUDA 12.1.
#
# Install:  pip install -r requirements.txt
#
# Two dependencies are NOT installable from a package index and must be
# installed separately AFTER this file (they are local/source builds):
#   1. pointnet2_ops  ->  run ./install_pointnet.sh  (or: cd pointnet2_ops && pip install --no-build-isolation .)
#   2. SAM-2          ->  install from source: pip install git+https://github.com/facebookresearch/sam2.git
#                        (only needed for the SAM2 segmentation server / rpc_server)

# --- Package indexes for CUDA wheels (must come first) ---
--extra-index-url https://download.pytorch.org/whl/cu121
--find-links https://data.pyg.org/whl/torch-2.1.0+cu121.html

# --- Core PyTorch (CUDA 12.1 builds) ---
torch==2.1.0+cu121
torchvision==0.16.0+cu121

# --- PyTorch Geometric stack (from the find-links index above) ---
torch-cluster==1.6.3+pt21cu121
torch-scatter==2.1.2+pt21cu121
torch-geometric==2.7.0

# --- Sparse conv ---
spconv-cu120==2.3.6

# --- Numerics / data ---
numpy==1.26.4
scipy==1.15.3
scikit-learn==1.7.2
h5py==3.16.0
pickle5==0.0.12
webdataset==1.0.2
tensordict==0.12.3
SharedArray==3.2.4

# --- ML / models ---
transformers==4.48.3
diffusers==0.11.1
timm==1.0.15
huggingface-hub==0.25.2

# --- Config / logging ---
hydra-core==1.4.0.dev1
omegaconf==2.4.0.dev11
addict==2.4.0
tensorboard==2.20.0
tensorboardX==2.6.5

# --- Geometry / mesh / robot models ---
trimesh==4.5.3
yourdfpy==0.0.56
scene-synthesizer[recommend]==1.15.0
usd-core==26.5

# --- Rendering / visualization ---
pyrender==0.1.45
PyOpenGL==3.1.0
meshcat==0.3.2
viser==1.0.28
matplotlib==3.10.9
imageio==2.37.3
opencv-python==4.10.0.84  # last line that allows numpy<2 (numpy==1.26.4 pin); cv2 usage is basic/stable
pillow==12.2.0

# --- Serving (ZMQ RPC) ---
pyzmq==27.1.0
msgpack==1.1.2
msgpack-numpy==0.4.8

# --- Tooling / misc ---
tqdm==4.67.3
ipython==8.39.0
yapf==0.40.1
pytest==9.0.3

# NOTE: grasp_gen/dataset/suction.py additionally imports `qpsolvers`, which is
# not installed in the reference environment. If you use suction grasping, also:
#   pip install qpsolvers
